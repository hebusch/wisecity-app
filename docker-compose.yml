services:
  gps:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      DB_PATH: /app/data/gps.duckdb
    volumes:
      - gps-data:/app/data
    restart: unless-stopped

volumes:
  gps-data:
